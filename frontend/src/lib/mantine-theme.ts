import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: 'Allen Institute Plus Text, sans-serif',
  headings: { fontFamily: 'Allen Institute Plus Headline, sans-serif' },
});
